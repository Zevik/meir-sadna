module.exports = {
  output: 'export',        // Required for static export
  images: { unoptimized: true }, // Necessary for static images
  trailingSlash: true      // Helps with clean URL paths
};
